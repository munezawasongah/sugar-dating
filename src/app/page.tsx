export default function HomePage() {
  return (
    <main style={{ padding: 40, fontFamily: "sans-serif" }}>
      <h1>Arrangement — API is live</h1>
      <p>
        This root page is a placeholder. Frontend screens (Discover, Messages,
        Profile) from the design preview still need to be built out as real
        React components under <code>src/app</code>.
      </p>
    </main>
  );
}
